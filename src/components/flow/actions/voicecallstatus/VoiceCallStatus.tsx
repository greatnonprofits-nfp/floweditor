import * as React from 'react';
import { VoiceCallStatusAction } from 'flowTypes';

const VoiceCallStatusComp: React.SFC<VoiceCallStatusAction> = (): JSX.Element => (
  <div>Voice Call Status</div>
);

export default VoiceCallStatusComp;
